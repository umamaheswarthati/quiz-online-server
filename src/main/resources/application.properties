
server.port=9192
#spring.datasource.url=jdbc:mysql://localhost:3306/quiz-online
#spring.datasource.username=root
#spring.datasource.password=admin

#spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.MySQLDialect
#spring.jpa.hibernate.ddl-auto=update

# PostgreSQL Database Connection
spring.datasource.url=jdbc:postgresql://localhost:5432/quiz
spring.datasource.username=postgres
spring.datasource.password=admin
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate/JPA Configuration (optional, if you're using Spring Data JPA)
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
# Or validate, create, create-drop, none depending on your needs
spring.jpa.show-sql=true
# Set to true to see the generated SQL statements in the console
